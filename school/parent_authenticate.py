from Admissions.models import Scholar
from django.shortcuts import render

#def checklogin(username=None, password=None):













   # if username is None:
    #    username = kwargs.get(Scholar.Contact_number)
    #try:
    #    user = Scholar.Contact_number._default_manager.get_by_natural_key(username)
    #except Scholar.Contact_number.DoesNotExist:
        # Run the default password hasher once to reduce the timing
        # difference between an existing and a nonexistent user (#20760).
    #    Scholar.Date_of_Birth().set_password(password)
    #else:
    #    if user.check_password(password):
     #       return user

